# Float64 represents a signed 64-bit IEEE-754 conform floating-point number
type std::Float64

attributes {
	*min Float64
	*max Float64
}

// Implementation is provided by the implementing engine
value Float64
